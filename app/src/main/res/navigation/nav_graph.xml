<?xml version="1.0" encoding="utf-8"?>

<!-- กำหนดหน้าต่าง ๆ และการเปลี่ยนหน้าภายในแอป -->
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <!-- Home -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.diceplanet.app.ui.home.HomeFragment"
        android:label="Home" />

    <!-- Boardgame -->
    <fragment
        android:id="@+id/boardgameFragment"
        android:name="com.diceplanet.app.ui.boardgame.BoardgameFragment"
        android:label="Board Games" />

    <!-- Board Game Detail -->
    <fragment
        android:id="@+id/boardgameDetailFragment"
        android:name="com.diceplanet.app.ui.boardgame.BoardgameDetailFragment"
        android:label="Board Game Detail" />

    <!-- Booking -->
    <fragment
        android:id="@+id/bookingFragment"
        android:name="com.diceplanet.app.ui.booking.BookingFragment"
        android:label="Booking" />

    <!-- Member -->
    <fragment
        android:id="@+id/memberFragment"
        android:name="com.diceplanet.app.ui.member.MemberFragment"
        android:label="Member" />

    <!-- Profile -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.diceplanet.app.ui.profile.ProfileFragment"
        android:label="Profile" />
</navigation>